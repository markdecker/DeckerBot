{
  "name": "Mark Decker Design Bot",
  "description": "Mark Decker Design, a resume bot.",
  "keywords": ["Mark Decker","UX Design", "bot"],
  "website": "https://markdeckerdesign.com",
  "repository": "https://github.com/markdecker/DeckerBot",
  "env": {
    "SMOOCH_APP_TOKEN": {
      "description": "Your Smooch app token which is used to initialize the web widget.",
      "required": true
    },
    "SMOOCH_KEY_ID": {
      "description": "Your Smooch key ID, used to talk to the Smooch API",
      "required": true
    },
    "SMOOCH_SECRET": {
      "description": "Your Smooch secret, used to talk to the Smooch API",
      "required": true
    }
  }
}
